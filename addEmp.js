// Dependencies
const mysql = require("mysql");
const start = require("./index.js");
const table = require("console.table");

// You need to create an array of objects where the name key is the manager's name and the value key is the manager ID, and pass that to the inquirer choices



module.exports = {allEmp};